# Curation report: wine

Raw acquired from `builtin:wine` (license CC BY 4.0 (UCI)); raw kept unchanged under `raw/`.

Processed CSVs written to `processed/` by `curate_benchmark_data.py`.

## wine_pca
Transforms: 13 chemical features x samples matrix; metadata=cultivar.
Mapping: `{'matrix_row_id': 'feature', 'metadata_key': 'sample_id', 'color': 'group'}`
