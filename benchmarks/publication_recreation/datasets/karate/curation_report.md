# Curation report: karate

Raw acquired from `builtin:karate` (license Public-Domain); raw kept unchanged under `raw/`.

Processed CSVs written to `processed/` by `curate_benchmark_data.py`.

## karate_network
Transforms: Edge list from networkx.karate_club_graph().
Mapping: `{'source': 'source', 'target': 'target', 'layout': 'spring', 'seed': 42}`
