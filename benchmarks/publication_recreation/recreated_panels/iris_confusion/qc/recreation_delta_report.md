# Recreation delta — iris_confusion

Recreated via Make My Figure `confusion_matrix` (Publication style) from real public data. This is a publication-grade, scientifically-traceable recreation, NOT an exact copy of any published figure image.

- Target (described, no image stored): Confusion matrix of true vs predicted species (5-fold CV predictions).
- Known differences: colors/exact layout differ from the source figure; axis ranges are data-driven; no image-similarity computed (no reference image stored).
