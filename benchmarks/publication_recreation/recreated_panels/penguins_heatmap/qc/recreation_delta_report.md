# Recreation delta — penguins_heatmap

Recreated via Make My Figure `heatmap_clustered_matrix` (Publication style) from real public data. This is a publication-grade, scientifically-traceable recreation, NOT an exact copy of any published figure image.

- Target (described, no image stored): Clustered heatmap of per-species mean morphometrics, z-scored per feature.
- Known differences: colors/exact layout differ from the source figure; axis ranges are data-driven; no image-similarity computed (no reference image stored).
