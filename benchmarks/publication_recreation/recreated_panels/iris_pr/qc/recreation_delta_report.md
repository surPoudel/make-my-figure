# Recreation delta — iris_pr

Recreated via Make My Figure `precision_recall_curve` (Publication style) from real public data. This is a publication-grade, scientifically-traceable recreation, NOT an exact copy of any published figure image.

- Target (described, no image stored): Precision-recall curve (+AUPRC) from the same cross-validated probabilities.
- Known differences: colors/exact layout differ from the source figure; axis ranges are data-driven; no image-similarity computed (no reference image stored).
