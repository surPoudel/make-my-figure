# Recreation delta — iris_roc

Recreated via Make My Figure `roc_curve` (Publication style) from real public data. This is a publication-grade, scientifically-traceable recreation, NOT an exact copy of any published figure image.

- Target (described, no image stored): ROC curve (+AUC) from 5-fold cross-validated logistic-regression probabilities.
- Known differences: colors/exact layout differ from the source figure; axis ranges are data-driven; no image-similarity computed (no reference image stored).
