# Visual QC - guo2019_melanoma_roc

**Result: PASS**

- Rendered through the app's normal `roc_curve` path (Publication style).
- Axes match the paper: Sensitivity (y) vs 1 - Specificity (x), unit square, diagonal chance line, AUC in the legend.
- Curve shape matches the published training-cohort ROC (steep early rise to ~0.8 sensitivity by ~0.2 FPR, then gradual).
- Exports non-empty: PNG/SVG/PDF.
