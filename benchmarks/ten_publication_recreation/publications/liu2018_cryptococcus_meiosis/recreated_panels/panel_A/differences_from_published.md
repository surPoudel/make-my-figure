# Differences from the published figure - liu2018_cryptococcus_meiosis

Liu L, et al. (2018). eLife 7:e38683. https://doi.org/10.7554/eLife.38683

Classification: **publication-grade recreation of the violin-plot KIND; same kind, not pixel-identical.**

- Distributions and medians match (every value plotted verbatim from the source data; n=150 per group).
- The paper colours violins by development stage (grey = unisexual, dark red = bisexual) with two bracketed blocks and a legend; the app has no hue channel for this plot type, so condition is encoded in the x label suffix (U/B) and the app's default per-violin palette is used.
- The paper marks selected pairwise comparisons 'n.s.'; the app draws no significance annotation here (it never draws a statistic it was not asked to compute).
- Colours, fonts and limits are Make My Figure Publication defaults.
