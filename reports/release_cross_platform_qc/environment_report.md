# Environment report — cross-platform QC

> Generated by the QC harness on the current session's platform. Usernames/private
> path segments are redacted. Re-run on each target OS to compare.

## This platform (current session)

- App version: `0.6.1`
- Git commit: `4aaae44`
- Module path (redacted): `/mnt/c/Users/<user>/OneDrive - St. Jude Children's Research Hospital/make_my_plot`
- In /mnt/c: `True` · In OneDrive: `True`
- Platform: `Linux-6.6.87.2-microsoft-standard-WSL2-x86_64-with-glibc2.35`
- Python: `3.11.8`
- Matplotlib backend (headless QC): `Agg` (interactive session backend: `Agg`)

## Package versions

| package | version |
|---|---|
| matplotlib | 3.10.8 |
| pandas | 2.2.3 |
| numpy | 2.1.2 |
| scipy | 1.17.1 |
| networkx | 3.6.1 |
| statsmodels | 0.14.6 |
| streamlit | not installed |
| PySide6 | 6.11.1 |

## Notes
- `/mnt/c` (Windows drive) and OneDrive folders are **slow** under WSL for file I/O;
  for active development prefer the WSL Linux filesystem (e.g. `~/dev/make_my_plot`).
- The QC harness uses the headless `Agg` backend for determinism; the interactive
  apps use the platform GUI backend.
- If journal-named styles or a single-color network appear, first confirm the commit
  hash above matches your pulled HEAD — a stale installed package can shadow the repo.
