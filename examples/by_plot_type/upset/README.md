# UpSet plot (set intersections) — example data

**Plot type id:** `upset_plot`

**Scientific use case:** Intersections among many sets (a scalable alternative to Venn diagrams).

> ⚠️ **Synthetic data.** This dataset was generated by
> `scripts/generate_example_data.py` (seed 42). It is plausible but NOT real
> and must not be cited as a biological finding. License: CC0-1.0.

## What a row represents

One row = one record for this analysis.

## Columns

- **Required:**
  - `DEG_up`
  - `DEG_down`
  - `Promoter_peak`
  - `Conserved`
- **Optional:**
  - `gene`

## Files

- `data.csv`, `data.tsv`, `data.xlsx` — the same table in three formats.
- `plotspec.json` — a ready-to-use PlotSpec (plot type, column mapping, style, output).
- Open any of these in the app via **Use example data → UpSet plot (set intersections)**.

## Replace with your own data

Use one 0/1 column per set (each row = one element), or long form (element + set columns). Set mapping['sets'] to your set columns.

Keep the **required column names** above (or remap columns in the app). Column
order does not matter — the app validates by name and type.

## Common mistakes to avoid

  - Membership columns must be 0/1 (or boolean).
  - Provide at least 2 sets.
